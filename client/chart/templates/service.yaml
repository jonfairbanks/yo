apiVersion: v1
kind: Service
metadata:
  name: {{ include "yo-client.fullname" . }}
  labels:
    {{- include "yo-client.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "yo-client.selectorLabels" . | nindent 4 }}
