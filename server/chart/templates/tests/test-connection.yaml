apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "yo-api.fullname" . }}-test-connection"
  labels:
{{ include "yo-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "yo-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
